{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "passThroughEnv": ["*"],
      "dependsOn": ["^build"],
      "outputs": [
        ".next/**",
        "!.next/dev/**",
        "!.next/cache/**",
        "dist/**"
      ]
    },
    "dev": {
      "passThroughEnv": ["*"],
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"]
    }
  }
}