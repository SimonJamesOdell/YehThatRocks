# Rate limiting zones for YehThatRocks
# player_api: applied to high-frequency player API endpoints
#   30 req/min per IP = 0.5r/s; burst=15 absorbs normal page-load clusters
limit_req_zone $binary_remote_addr zone=player_api:10m rate=30r/m;
